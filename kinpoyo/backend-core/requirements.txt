# ── Web Framework ──────────────────────────────────────────
fastapi==0.137.2
uvicorn[standard]==0.48.0
python-multipart==0.0.20      # ファイルアップロード・フォームデータ

# ── 設定・環境変数 ─────────────────────────────────────────
python-dotenv==1.1.0
pydantic-settings==2.9.1

# ── データベース ───────────────────────────────────────────
sqlalchemy==2.0.41
alembic==1.16.1               # マイグレーション管理
psycopg[binary]==3.3.4        # PostgreSQL ドライバ（Python 3.14対応 psycopg3）

# ── 認証 ──────────────────────────────────────────────────
python-jose[cryptography]==3.5.0   # JWTトークン
passlib[bcrypt]==1.7.4             # パスワードハッシュ化
bcrypt==4.0.1                      # passlib1.7.4がbcrypt4.1+のAPI変更に未対応のため固定
