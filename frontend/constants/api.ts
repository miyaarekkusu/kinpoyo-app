// Backend API base URL.
// Change this to your PC's local IP when testing on a real device.
// Find it with `ipconfig` (IPv4 address) and make sure the iPhone is
// on the same Wi-Fi as the PC.
export const API_URL = 'http://10.141.114.26:8000';
